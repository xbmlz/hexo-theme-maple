<div class="posts">
  <ul>
    <% page.posts.each(function (post) { %>
    <a href="<%- url_for(post.path) %>" class="item">
      <li>
        <div class="title"><%= post.title %></div>
        <div class="time"><%- date(post.date, "YYYY-MM-DD HH:MM") %></div>
        <div class="time"><%= post.tag %></div>
      </li>
    </a>
    <% }) %>
  </ul>
</div>
